//
// https://levelup.gitconnected.com/rust-with-visual-studio-code-46404befed8
//

fn main() {
   let mut vector1: Vec<i32> = Vec::new();
   let mut vector2: Vec<i32> = vec![1,2,3,4,5];
   let mut vector1: Vec<i32> = Vec::with_capacity(10);

   let values = vec![1,2,3];
   for a in values {
       println!("{}", a);
   }

   let array = ["This","is","an","array"];
   let array_slice = &array[0..4];

}

//
